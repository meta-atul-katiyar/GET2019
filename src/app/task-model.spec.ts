import { TaskModel } from './task-model';

describe('TaskModel', () => {
  it('should create an instance', () => {
    // expect(new TaskModel(taskName, taskDescription, taskPriority, status, creationDate, completionDate)).toBeTruthy();
  });
});
